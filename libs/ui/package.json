{
  "name": "@repo/ui",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "clean": "npx shx rm -rf dist coverage",
    "test": "npx jest",
    "lint": "npx eslint ./src",
    "check-types": "npx tsc -p tsconfig.lib.json --noEmit",
    "prebuild": "npm run check-types"
  },
  "dependencies": {
    "@repo/authorization": "*"
  }
}
